import std/[os, strutils, random, terminal, sequtils]

# var s: seq[int]

# var topthree = s[(max(0, s.high-2))..(s.high)]
# echo topthree

# proc setFGC(s: ForegroundColor) =
#     setForegroundColor(s)
#     stdout.setStyle({styleBright})

# setFGC(fgWhite)
# stdout.write("input > ")
# var i = getch()
# echo i.int

echo -1 mod 7